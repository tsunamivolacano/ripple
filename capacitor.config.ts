import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.rippleactivitylogger',
  appName: 'Ripple Activity Logger',
  webDir: 'dist'
};

export default config;
